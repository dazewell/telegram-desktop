# Copy this file to ".env" in the repository root and fill in your Telegram
# API credentials. The real ".env" is gitignored, so your secrets are never
# committed.
#
#   Copy-Item .env.example .env
#
# Get api_id / api_hash here: https://my.telegram.org/apps
TDESKTOP_API_ID=
TDESKTOP_API_HASH=
